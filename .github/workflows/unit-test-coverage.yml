name: Unit Test and Coverage

on:
  push:
  pull_request:

jobs:
  unit-test-coverage:
    name: Run unit test and coverage
    uses: havencarlson/cFS/.github/workflows/unit-test-coverage.yml@workflow-ubuntu-update
